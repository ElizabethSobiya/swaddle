{
  "name": "swaddle",
  "private": true,
  "workspaces": [
    "client",
    "shared/types"
  ],
  "scripts": {
    "dev": "npm --workspace @swaddle/client run dev",
    "test": "npm --workspace @swaddle/client run test"
  }
}
